name: Sync GitHub Project

on:
  workflow_call:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Placeholder - substituir pela versão final após validar PROJECT_TOKEN"
